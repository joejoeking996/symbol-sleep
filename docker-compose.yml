services:
  website:
    image: node:22.17.0-alpine
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    env_file:
      - .env

volumes:
  node_modules:
